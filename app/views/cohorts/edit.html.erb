<h1>Edit Cohort</h1>

<%=render 'forms'%>
<br>
<%=link_to 'Delete Cohort', course_cohort_path(@course.id, @cohort.id), method: :delete, data: { confirm: "Are you sure you want to delete #{@cohort.name} ?"}%><br>
<%= link_to "Return to #{@cohort.name}", course_cohort_path(@course.id, @cohort.id)%>
